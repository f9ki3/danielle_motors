<?php
$current_folder = basename($_SERVER['REQUEST_URI']);
// if($current_folder === "Manage-Inventory"){ echo "active"; } else { echo "";}
// if($current_folder === "Purchased-Order-Supplier"){ echo "active"; }